# ====================
# Lock the system
# ====================
# lock with a picture:
# bindsym $mod+l exec i3lock -i  ~/.config/i3/i3-lock-screen.png -p default|win -t
# bindsym $mod+l exec i3lock -p default|win -t
# lock by blurring the screen:
bindsym $mod+l exec ~/.config/i3/scripts/blur-lock
